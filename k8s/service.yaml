apiVersion: v1
kind: Service
metadata:
  name: my-portfolio-service
spec:
  selector:
    app: my-portfolio
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007
