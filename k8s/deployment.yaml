apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-portfolio-deployment
spec:
  replicas: 2
  template:
    spec:
      containers:
      - name: my-portfolio
        image: gammala/my-portfolio:latest 
        ports:
        - containerPort: 80
