apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-portfolio-deployment
spec:
  replicas: 2
  template:
    spec:
      containers:
      - name: Final-Project
        image: gammala/Final-Project:latest 
        ports:
        - containerPort: 80
